# SQLModel и углубленная работа с транзакциями


## Введение в Relationship Attributes


На предыдущем занятии мы реализовали новую сущность `Item` и реализовали
межтабличную связь между таблицами `items` и `users`. Связь эта реализована
с помощью внешнего ключа `user_id` в таблице `items`, который ссылается на
`id` из таблицы `users`. Таким способом мы реализуем связь один-ко-многим,
показывая, что у одного пользователя может быть много `items`. Соответственно,
если мы удалим столбец `user_id` из таблицы `items`, то связь перестанет существовать.

Сегодня мы добавим в наши модели так называемые `Relationship Attributes`.

> Relationship Attributes - атрибуты SQLModel-моделей, описывающие и обеспечивающие
объектный доступ (через .) к связанным записям и поддерживающие двустороннюю
синхронизацию связи между моделями в соответствии с внешними ключами.

С помощью этих атрибутов мы не будем создавать новые столбцы напрямую в БД.
Значение в `Relationship Attributеs` - это фактическое значение, к которому 
относится данный атрибут. Например, для сущности `User`, если мы обратимся
к `user`.`items`, то получим весь список объектов `Item`, которые принадлежат
данному пользователю. Так мы упрощаем себе работу с ORM. 


---


## Функция Relationship() и циклические импорты


Начнем внедрять `Relationship Attributes` в код наших моделей `User` и `Item`.

Сначала пойдем, например, в `app`/`models`/`users`.py и импортируем функцию `Relationship()`
из модуля `SQLModel`. Именно с помощью `Relationship()` мы сможем объявлять `Relationship Attributes`.


```python
# app/models/users.py
from sqlmodel import Field, SQLModel, Relationship
...
```

Затем в классе `User` добавим новый атрибут `items`, обозначающий список объектов
`Item` для конкретного пользователя. Именно тут нам и пригодится функция `Relationship()`:


```python
# app/models/users.py
class User(UserBase, table=True):
    ...
    items: list["Item"] = Relationship(back_populates="user")
```

Обсудим 2 важные в написанной строчке:
1) `list["Item"]` - мы указали такой тип, т.к. у одного `User` может быть много объектов `Item`.
`"Item"` указан в кавычках, чтобы сослаться на класс, который объявлен в другом файле.

2) За счет `back_populates="user"` поле `items` связывается с полем `user` в модели `Item`. 
Мы это поле еще не создали, но позже создадим 😁 Так, если мы добавим новый item
в `user.items`, в `item.user` автоматически выставится данный `user` и наоборот.

> СОВЕТ ПРЕПОДАВАТЕЛЮ: среда разработки может подсвечивать фрагмент `"Item"` с комментарием, 
что `"Item"` не определено. Это нормально, ниже мы об этом поговорим.

Пока что с `app/models/users.py` разобрались, теперь пойдем и реализуем то же самое, но в `app/models/items.py:`


```python
# app/models/items.py
from sqlmodel import Field, SQLModel, Relationship
...
class Item(ItemBase, table=True):
    ...
    user: User = Relationship(back_populates="items")
```

С `Relationship()` разобрались, но есть некоторые проблемы в текущем коде.
В `app/models/users`.py мы обращаемся к классу `Item`, которого нет в этом модуле.
И также в `app/models/items`.py мы обращаемся к классу `User`, которого тоже нет в этом модуле.

Вроде бы, чтобы решить проблему, нам нужно в модуль `items` импортировать класс `User`,
а в `users` - класс `Item`. Попробуем так и сделать - добавим нужные импорты:


```python
# app/models/users.py
from app.models.items import Item
...
```

```python
# app/models/items.py
from app.models.users import User
...
```

Если мы сейчас попробуем запустить приложение, то увидим следующую ошибку:
`ImportError: cannot import name 'User' from partially initialized module 'app.models.users'`

А это ни что иное, как циклический импорт.

> Циклический импорт - ситуация, когда два и более модулей зависят друг от друга,
создавая цикл зависимостей. Например, модуль `items` импортирует модуль `users`, 
который, в свою очередь, импортирует модуль `items`. В результате может появиться подобная ошибка:
`ImportError: cannot import name 'User' from partially initialized module 'app.models.users'`

В официальной документации по `SQLModel` предлагается следующее решение данной проблемы.
Можно воспользоваться специальной переменной `TYPE_CHECKING` из встроенного модуля `typing`.
Эта переменная имеет значение `True`, когда код не работает, но может проверяться специальными
инструментами для статического анализа кода. О них мы как-нибудь поговорим на курсе 😉
Но когда код запущен, то это переменная имеет значение `False`. Как же использовать эту переменную?
Рассмотрим на примере `app/models/users.py`. Сначала подключим эту переменную в коде:


```python
# app/models/users.py
from typing import TYPE_CHECKING
...
```

Затем ниже укажем условие - если значение переменной `TYPE_CHECKING` равно `True`, то мы
выполняем `from app.models.items import Item`:


```python
# app/models/users.py
if TYPE_CHECKING:
    from app.models.items import Item
```

И все, проблема цикличного импорта решается! То есть для среды разработки импорт работает,
но во время выполнения программы этот импорт не запускается. Проделаем то же самое для `app/models/items.py`: 


```python
# app/models/items.py
from typing import TYPE_CHECKING
...
if TYPE_CHECKING:
    from app.models.users import User
```

Только есть одно важное дополнение для `app/models/items.py`. В строчке с объявлением атрибута `user` внутри 
класса `Item`, класс `User` нужно обязательно взять в кавычки, иначе будет ошибка:


```python
# app/models/items.py
class Item(ItemBase, table=True):
    ...
    user: "User" = ...
```

Супер, с циклическими импортами разобрались! Теперь, если мы запустим приложение, то ошибки уже не будет.


---


## Обновление репозитория и эндпоинтов


Ранее мы добавили в наши ORM-модели работу с функцией `Relationship()`, теперь применим
это для репозитория и эндпоинтов. Начнем с эндпоинта `POST /users/{user_id}/items`, где мы 
создаем `item` для указанного пользователя. Наша задача - внедрить в него использования
объектного подхода, доступного с помощью внедренного ранее `Relationship()`.

Для начала обратимся к репозиторию, а именно к файлу `app/repositories/items.py`.
Внутри файла есть функция `create_item()`. Для использования объектного подхода, вместо
`user_id` мы будем работать с объектом `user`, экземпляром класса `User`. Для этого
сначала импортируем `User` в начале файла: 


```python
# app/repositories/items.py
from app.models.users import User
...
```

Затем в самой функции сделаем следующие корректировки:
1) В заголовке функции, параметр `user_id: UUID` заменим на `user: User`.
2) При создании экземпляра класса `Item` вместо `user_id=user_id` укажем `user=user`.


```python
# app/repositories/items.py
async def create_item(session: AsyncSession, user: User, item_data: ItemCreate):
    new_item = Item(**item_data.model_dump(), user=user)
    ...
```

Теперь мы связываем item с `user` объектно, а `user_id` проставится автоматически.
Репозиторий обновлен, теперь пойдем к обновлению самого эндпоинта `POST /users/{user_id}/items`.
Нам нужен файл `app/routes/users.py`, где лежит функция `create_user_item()`.
Идея такая же - при вызове функции `create_item_repository()` вместо `user_id=user_id` укажем `user=user`.


```python
# app/routes/users.py
...
new_item = await create_item_repository(session=session, user=user, item_data=item_data)
...
```

Итак, изменения внесены, попробуем запустить наше приложение и создать новый `item` 
с привязкой с конкретному `user`.

> СОВЕТ ПРЕПОДАВАТЕЛЮ: создайте новый `item` с помощью эндпоинта `POST /users/{user_id}/items`,
убедитесь, что все работает. Дополнительно проверьте изменения в БД с помощью DBeaver.

Может показаться, что мы просто переписали одно на другое и никаких глобальных изменений нет.
Для нашего несложного кода - да, глобальных изменений нет, но при этом использование `Relationship()` 
помогает лучше понять смысл кода и снижает вероятность ошибок, что крайне важно в более
сложных программах, которые нас ждут дальше на курсе 😉

Двигаемся дальше и попробуем создать новый эндпоинт, задача которого - обновить существущий `item` и, 
в частности, перенести `item` другому пользователю. Такой эндпоинт может быть полезен, 
когда право владения объектом переходит от одного человека к другому. 
Например, `User` - это сотрудник, а `Item` - это задача. 
Если сотрудник уходит в отпуск, то задачу нужно передать другому сотруднику.

Начнем реализацию данного эндпоинта с модели обновления `Item`, а именно - `ItemUpdate`.
Сейчас через данную модель можно передать только `title` и `description`.
Чтобы можно было сменить владельца, добавим опциональное поле `user_id`:


```python
# app/models/users.py
class ItemUpdate(ItemBase):
    title: str | None = Field(default=None, min_length=1, max_length=128)
    user_id: UUID | None = None
```

С моделью разобрались, переходим к слою репозитория. Т.к. здесь главной сущностью является именно `item`,
то и функцию репозиторного слоя будем размещать в `app/repositories/items.py`. По своей сути мы будем
обновлять уже существующий `item`, поэтому назовем функцию `patch_item()`. Также не забудем импортировать
модель `ItemUpdate`.

СОВЕТ ПРЕПОДАВАТЕЛЮ: на предыдущем занятии у студентов было ДЗ по добавлению возможности удаления
`item` по ID. Соответствующие функции должны быть в файлах `app/repositories/items.py` и `app/routes/items.py`.
Если на занятии останется время, то в конце занятия реализуйте эти функции, чтобы у всех студентов
удаление `item` было в проектах. Если времени не хватает, то реализуйте эти функции в начале следующего занятия.


```python
# app/repositories/items.py
from app.models.items import Item, ItemCreate, ItemUpdate
...
async def patch_item(
    session: AsyncSession,
    item_db: Item,
    item_data: ItemUpdate,
    new_user: User | None = None
) -> Item:
    # обновляем связь через Relationship(), если передан новый владелец
    if new_user is not None:
        item_db.user = new_user
    # обновляем остальные поля, кроме user_id
    data = item_data.model_dump(exclude_unset=True, exclude={'user_id'})
    item_db.sqlmodel_update(data)

    session.add(item_db)
    await session.commit()
    await session.refresh(item_db)
    return item_db
```

Со слоем репозитория разобрались, теперь идем реализовывать эндпоинт в файле `app/routes/items.py`.
Внутри предусмотрим следующие крайние случаи:
- Если `item` с указанным ID не найден, то возвращаем статус 404 с сообщением `'Item not found'`
- Если клиент в JSON для `user_id` передал `null`, то возвращаем статус 422 с сообщением `'user_id cannot be null'`
- Если `user` с указанным ID не найден, то возвращаем статус 404 с сообщением `'User not found'`
Не забудем также подключить созданную ранее функцию `patch_item()` и модель `ItemUpdate`.
А т.к. нам нужно будет еще проверять наличие пользователя с указанным ID, то подключим еще
и функцию `get_user()`.


```python
# app/routes/items.py
from app.models.items import ItemOut, ItemUpdate
from app.repositories.items import get_item, delete_item, patch_item
from app.repositories.users import get_user
...
@router.patch('/{item_id}', response_model=ItemOut)
async def patch_item_by_id(item_id: UUID, item_data: ItemUpdate, session: SessionDep):
    item_db = await get_item(session, item_id)
    if item_db is None:
        raise HTTPException(status_code=404, detail='Item not found')
    
    payload = item_data.model_dump(exclude_unset=True)
    new_user = None

    # если клиент хочет сменить владельца, то он должен явно прислать user_id
    if 'user_id' in payload:
        # запрещаем клиенту отправлять null для user_id
        if item_data.user_id is None:
            raise HTTPException(status_code=422, detail='user_id cannot be null') 
        
        new_user = await get_user(session, item_data.user_id)
        if new_user is None:
            raise HTTPException(status_code=404, detail='User not found')
    
    updated_item = await patch_item(
        session=session,
        item_db=item_db,
        item_data=item_data,
        new_user=new_user
    )
    return updated_item
```

Запустим наше приложение и проверим работу нового эндпоинта.

>СОВЕТ ПРЕПОДАВАТЕЛЮ: обновите владельца для любого существующего у вас `item`.
Попробуйте изменить значения и для других атрибутов, `title` и/или `description`.
При тестировании эндпоинта `PATCH /items/{item_id}` в разделе `Edit Value` для поля
`"user_id"` вы увидите значение по умолчанию `"3fa85f64-5717-4562-b3fc-2c963f66afa6"`.
Не переживайте, это стандартный `UUID` из примеров OpenAPI. 
Swagger UI подставляет его автоматически, когда видит поле типа `UUID`.
Замените это значение на нужный вам `UUID`.
